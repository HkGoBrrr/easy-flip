/* Copy this file to config.js and fill it in. config.js is gitignored —
 * it holds your keys and is the only file you edit after a rebuild.
 */
window.CONFIG = {
  // Mapbox public token (pk...). Restrict it to your domain in the Mapbox
  // dashboard. Leave empty to fall back to Photon, which needs no key.
  MAPBOX_TOKEN: "",

  // Your Cloudflare Worker URL, no trailing slash. Leave empty to turn off
  // property autofill; everything else keeps working.
  PROPERTY_API: ""
};
